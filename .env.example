# ============================================
# Backend Environment Variables
# ============================================
# Copy this file to .env.local for local development
# For production, set these in your deployment environment

# ============================================
# Server Configuration
# ============================================
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:3000

# ============================================
# Authentication & Security
# ============================================
# NextAuth.js Configuration
# Generate a secure random string for production:
# openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-change-in-production
NEXTAUTH_URL=http://localhost:3000

# Google OAuth Configuration
# Option 1: Use environment variables (recommended for production)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Option 2: Use google_authen.json file (for local development)
# Place google_authen.json in the project root with your OAuth credentials

# Admin Access Control
# Comma-separated list of admin email addresses
ADMIN_EMAILS=admin@example.com,admin2@example.com
# Comma-separated list of admin email domains (e.g., @yourdomain.com)
ADMIN_DOMAINS=yourdomain.com

# ============================================
# Database Configuration
# ============================================
# Prisma uses SQLite by default (database.db)
# For production, you may want to use PostgreSQL or MySQL
# DATABASE_URL="file:./database.db"

# ============================================
# AI & External Services
# ============================================

# LiteLLM Configuration (for AI features)
LITELLM_API_KEY=sk-your-litellm-api-key
LITELLM_BASE_URL=http://localhost:4010

# OpenAI API Key (fallback if LiteLLM not configured)
OPENAI_API_KEY=sk-your-openai-api-key

# Alpha Vantage API (for earnings data)
# Get your free API key at: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key

# ============================================
# Vnstock API Integration (Vietnamese Stocks)
# ============================================
# Vnstock FastAPI service URL
VNSTOCK_API_URL=http://localhost:8002

# Vnstock Authentication (choose one method)
# Method 1: Use pre-generated JWT token
VNSTOCK_API_TOKEN=your-jwt-token-here

# Method 2: Use username/password (will auto-login)
VNSTOCK_API_USERNAME=your-username
VNSTOCK_API_PASSWORD=your-password
